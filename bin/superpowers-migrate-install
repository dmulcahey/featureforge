#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
INSTALL_BIN="$SCRIPT_DIR/superpowers-install-runtime"

if [[ ! -x "$INSTALL_BIN" ]]; then
  echo "ERROR: staged runtime install helper not found at $INSTALL_BIN" >&2
  exit 1
fi

exec "$INSTALL_BIN" "$@"
