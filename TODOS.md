# FeatureForge TODOs

- Add a hard "no new runtime deps in generated skill/runtime flows" policy: document it in repo instructions and skill-authoring guidance, then enforce it with a generated-skill contract test that scans bash preambles and inline runtime flows for forbidden interpreter/tool additions such as `python`, `python3`, `node`, `jq`, `perl`, and `ruby`; when shell consumers need structured helper output, prefer new runtime-owned scalar/field modes like `featureforge repo runtime-root --path` instead of teaching skill files to parse JSON with extra tooling.
- Make `requesting-code-review` always run through a fresh subagent review path: update the workflow rules and skill docs so a review request never reuses the main implementation context, require the controller to spawn or resume a dedicated reviewer before any final-approval claim, and add contract coverage that fails if review is satisfied without a subagent-owned result linked to the current diff or plan state.
- Resolve the `plan-eng-review` repo-safety write-target drift: the generated skill text still instructs `repo-file-write`, but the runtime CLI contract in this checkout exposes `plan-artifact-write` for plan-body mutations and `approval-header-write` for the approval flip; pick one authoritative naming scheme, update the template/generated docs or add an intentional runtime compatibility alias, and add contract coverage that fails when skill guidance and repo-safety write-target support diverge again.
- Add a per-step independent review gate to plan execution: after each implementation step in a task is done, require a fresh dedicated reviewer before the step can be treated as complete; keep review/remediation looping under the existing cycle-break logic until the reviewer is green with no findings, then run verification-before-completion and only then allow the next task step or next task to start; add runtime and skill-contract coverage that fails if execution can advance past a completed step without that green review receipt.
